<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
  <h1>Dogs! </h1>
  <% if (user) { %>
    <a href="/dogs/new">Add Dog</a> 
  <% } %>  
  <h2>All the Dogs!</h2>
	<ul>
  <% dogs.forEach(dog => { %>
    <li>🐕<%= dog.name %> is <%= dog.approach %></li>
  <% }) %>
  </ul>
</section>

<%- include('../partials/footer') %>